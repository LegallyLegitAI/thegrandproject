diff --git a/app/lib/types.d.ts b/app/lib/types.d.ts
new file mode 100644
index 0000000..abc1234
--- /dev/null
+++ b/app/lib/types.d.ts
@@
+export type Action = {
+  type: string;
+  payload?: any;
+};
+
+export type AppState = {
+  isLoggedIn: boolean;
+  user?: string;
+  healthCheck: boolean;
+  docStudio?: any;
+};
diff --git a/.eslintrc.js b/.eslintrc.js
new file mode 100644
index 0000000..def5678
--- /dev/null
+++ b/.eslintrc.js
@@
+module.exports = {
+  parser: '@typescript-eslint/parser',
+  extends: [
+    'eslint:recommended',
+    'plugin:@typescript-eslint/recommended',
+    'next/core-web-vitals'
+  ],
+  plugins: ['@typescript-eslint'],
+  parserOptions: {
+    ecmaVersion: 2020,
+    sourceType: 'module',
+  },
+  rules: {
+    'semi': ['error', 'always'],
+    'quotes': ['error', 'single'],
+  },
+};
diff --git a/jest.config.js b/jest.config.js
new file mode 100644
index 0000000..789fedc
--- /dev/null
+++ b/jest.config.js
@@
+module.exports = {
+  preset: 'ts-jest',
+  testEnvironment: 'node',
+  testMatch: [
+    '**/__tests__/**/*.[jt]s?(x)',
+    '**/?(*.)+(spec|test).[jt]s?(x)'
+  ],
+  moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json', 'node'],
+  transform: {
+    '^.+\\.(ts|tsx)$': 'ts-jest'
+  },
+  testPathIgnorePatterns: ['/node_modules/', '/.next/'],
+};
